extends layout

block content
    div.Container.blogPost
        
        section.info
            h1.title #{title}
            p.when #{post.when.format("MMM Do YYYY")}    
        .body !{body}

    