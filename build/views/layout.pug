html
    head
        if title
            title= `${title} | ${helpers.siteName}`
        else
            title= `${helpers.siteName}`        
        link(rel='stylesheet', href=`/${siteRoot}style.css`)
        link(href="https://netdna.bootstrapcdn.com/font-awesome/3.2.1/css/font-awesome.min.css" rel="stylesheet")
        //-link(rel="shortcut icon" type="image/png" href="/images/icons/doughnut.png")
        meta(name="viewport" content="width=device-width, initial-scale=1")        
        meta(charset="UTF-8")

    body
        //-pre= helpers.dump(helpers)
        header
            .inner 
                h1
                    a(href="/index.htm") James Mahoney
                nav
                    ul
                        li                           
                            a(href=`${siteRoot}/about.htm`) about
                        li
                            a(href=`${siteRoot}/index.htm`) blog            
        div.wrapper
            block allContent
                main.content
                    block content
                
        block footer            