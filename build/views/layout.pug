html
    head
        if title
            title= `${title} | ${helpers.siteName}`
        else
            title= `${helpers.siteName}`
        link(rel='stylesheet', href=`${siteRoot}/current.css`)
        link(rel='stylesheet', href=`${siteRoot}/local.css`)
        link(rel='stylesheet', href=`/${siteRoot}site.css`)
        //-link(rel="shortcut icon" type="image/png" href="/images/icons/doughnut.png")
        meta(name="viewport" content="width=device-width, initial-scale=1")        
        meta(charset="UTF-8")

    body
        //-pre= helpers.dump(helpers)
        header            
            a.site-title(href=`${siteRoot}/index.htm`) Code
            a.page-link(href=`${siteRoot}/about.htm`) Words
            a.page-link(href=`${siteRoot}/about.htm`) Cymraeg
            a.page-link(href=`${siteRoot}/about.htm`) About

        main.page-content
            .wrapper
                block content

        block footer
            footer.site-footer
                .wrapper
                    .footer-col-wrapper
                        .footer-col.footer-col-1
                            ul.contact-list
                                li 
                                    a(href="mailto:mahoneyjames+web@gmail.com") Email me                                
                        .footer-col.footer-col-2
                            ul.social-media-list
                                li 
                                    a(href="https://twitter.com/theblacksparrow") twitter: theblacksparrow                                
                        .footer-col.footer-col-3
                            p 
